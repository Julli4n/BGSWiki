# This is a title blacklist. Titles and users that match a regular expression here cannot be created.
# Use "#" for comments.
# This is case insensitive by default
# Remember that it matches the entire string, not just patterns. Add ".*" to the start and end of each regexp.

# Trade filter
(?!User:).*t+r+((a|4)+)?d+((e|3)+)?((i|1)+)n+(g+)?.* # e.g: trading_pets, should not include User:trading_pets


# Robux filter
.*f+r+e+.*(r|b)+o+b+u+x+.* # e.g: freeeee_robux or User:Free_robux_at_roblox_com
